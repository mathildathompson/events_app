<!-- in this file I want to show all the events that a user is going to. And also then show underneath, all the other people attending that event -->
<div id = "matches">
<h3>Your talks</h3>    
<% @current_user.attending.each do |events| %>

    <ul>
        <div class="panel panel-danger">
            <div class="panel-heading">
                <li class="panel-title"><a href="<%= events.event.url %>"><%= events.event.name %></a></li></span>
            </div> 
                <ul>
                    <div class="panel-body">   
                    <li>Also interested:</li>                           
                     <% events.event.users.each do |user| %>                       
                        <span>
                            <%= user.first_name unless user.first_name == @current_user.first_name && user.attending.select { |x| x.attending == false } %>
                        </span>                                      
                     <% end %>
                     </div>
                    
                </ul> 
            
        </div>       
    </ul>

<% end %>
</div>


    

        



